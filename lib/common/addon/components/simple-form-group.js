import Component from '@ember/component';
import layout from '../templates/components/simple-form-group';

export default Component.extend({
  layout,
  classNames: ['form-group', 'form-group-info']
});
