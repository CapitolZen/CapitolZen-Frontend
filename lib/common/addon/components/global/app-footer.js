import Component from '@ember/component';
import layout from '../../templates/components/global/app-footer';

export default Component.extend({
  layout,
  tagName: 'footer',
  classNames: ['container-fluid']
});
