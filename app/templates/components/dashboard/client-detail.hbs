{{#ui/ui-box as |b|}}
  {{#b.body}}
    <div class="row">
      <div class="col-2">
        {{ember-initials
          image=group.avatar
          name=group.title
          defaultName="?"
          seedText=group.title
          textColor="white"
          fontSize=45
          fontWeight=100
          fontFamily="Helvetica Neue Light, Arial, sans-serif"
          size=80
          class="img-thumbnail rounded-circle mr-3"
        }}
      </div>
      <div class="col-10">
        <h3>
          {{group.title}}
        </h3>
        {{#if actionsCount}}
          {{#bs/link-button "todos" (query-params group=group.id) type="primary" class="pull-right btn-sm"}}
            To-Do's <span class="badge badge-light">{{actionsCount}}</span>
          {{/bs/link-button}}
        {{/if}}
        {{#bs/link-button "groups.detail.index" group.id type="light" class="btn-sm"}}{{fa-icon "address-book"}} View{{/bs/link-button}}
        {{#bs/link-button "groups.detail.bills" group.id type="light" class="btn-sm"}}{{fa-icon "floppy-o"}} Saved Bills{{/bs/link-button}}
        {{#bs/link-button "reports.client" group.id type="light" class="btn-sm"}}{{fa-icon "file-text"}} Reports{{/bs/link-button}}
      </div>
    </div>
  {{/b.body}}
{{/ui/ui-box}}
