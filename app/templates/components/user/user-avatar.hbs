{{ember-initials
  image=user.avatar
  name=user.name
  defaultName="?"
  seedText=user.name
  textColor="white"
  fontSize=30
  fontWeight=100
  fontFamily="Helvetica Neue Light, Arial, sans-serif"
  size=size
}}