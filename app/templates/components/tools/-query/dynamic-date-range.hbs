
{{#if isEditing}}
  <h6>Dynamic Date Filter</h6>
  <div class="row">
    <div class="col-4">
      {{#power-select
        options=propertyOptions
        selected=dateKey
        searchEnabled=false
        onchange=(action (mut dateKey))
      as |property|}}
        <span class="text-capitalize">{{property}}</span> Date
      {{/power-select}}
    </div>
    <div class="col-4">
      {{#power-select
        options=operatorOptions
        selected=operator
        searchEnabled=false
        onchange=(action (mut operator))
      as |op|
      }}
        <span class="text-capitalize">{{op}}</span> Than
      {{/power-select}}
    </div>
    <div class="col-4">
      {{#power-select
        options=valueOptions
        selected=valueLabel
        searchEnabled=false
        onchange=(action (mut valueLable))
      as |val|
      }}
        Last <span class="text-capitalize">{{val}}</span>
      {{/power-select}}
    </div>
  </div>
  <div class="row">
    <div class="col-4">
      {{#bs-button type="secondary" onClick=(action "save")}}
        {{fa-icon "plus"}} Add
      {{/bs-button}}
    </div>
  </div>

{{else}}
  Filter bills with a {{dateKey}} {{operator}} than last {{valueLabel}}
  {{#bs-button type="secondary" size="sm" onClick=(action (mut isEditing) true)}}
    Edit
  {{/bs-button}}
{{/if}}