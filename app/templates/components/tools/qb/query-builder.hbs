{{yield
  (hash
    row=(component "tools/qb/filter-item" properties=properties queryResult=query)
    result=(component "tools/qb/query-results" properties=properties queryResult=query)
    view=(component "tools/qb/query-view" properties=properties queryResult=query)
    add=(component "tools/qb/filter-item" properties=properties queryResult=query isEditing=true)
  )
}}
