{{#ui/ui-box as |b|}}
  {{#b.header}}
    {{displayTitle}}: {{referencedModel.stateId}}
    {{close-button (action "dismiss") class="pull-right"}}
  {{/b.header}}
  {{#b.body}}
    {{#if referencedModel.sponsor}}
      <div class="w-100">
        {{#legislator/legislator-info legislator=referencedModel.sponsor class="clearfix pr-2" as |l|}}
           <strong>{{l.name class="d-inline" tagName="span"}}</strong> {{l.party class="d-inline" tagName="span"}}
        {{/legislator/legislator-info}}
      </div>
      <hr class="mt-0">
    {{/if}}
    {{#if referencedModel.currentCommittee}}
      <p class="card-text"><strong>Committee:</strong> {{referencedModel.currentCommittee.displayName}}</p>
    {{/if}}
    <p class="card-text">{{referencedModel.title}}</p>
  {{/b.body}}
  {{#b.footer class=footerClasses}}
    {{#if referencedModel}}
      {{bills/add-to-group
        bills=referencedModel
        buttonType="outline-primary"
        buttonText="Save"
        fullWidth=false
        billAdded=(action "dismiss")
      }}
      {{#link-to "bills.detail" referencedModel.id class="btn btn-outline-info ml-1"}}View{{/link-to}}
    {{/if}}
    {{bs-button defaultText="Dismiss" type="outline-secondary" class="mx-1" onClick=(action "dismiss")}}
  {{/b.footer}}
{{/ui/ui-box}}
