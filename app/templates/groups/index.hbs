{{ui-page-property "Clients" property="title"}}

{{#link-to "groups.add" class="btn btn-primary"}}Add Client{{/link-to}}
<div class="row mt-1">
  {{#each model as |group|}}
    <div class="col-lg-6 mt-md-3">
      <div class="card">

        <div class="card-block">
          <h4 class="card-title">
            {{#link-to "groups.detail" group.id}}
              {{#if group.logo}}
                <img src="{{group.logo}}" alt="{{group.title}}" class="img-thumbnail w-25">
              {{/if}}
              {{group.title}}
            {{/link-to}}
          </h4>
          {{ui/group-nav group=group view=true}}
            {{#if group.description}}
            <p class="card-text text-muted">{{group.description}}</p>
          {{/if}}
        </div>
      </div>
    </div>
  {{/each}}
</div>

