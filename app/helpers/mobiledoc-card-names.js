import { helper } from '@ember/component/helper';

export function mobiledocCardNames() {
  return ['tools/editor/-cards/image', 'tools/editor/-cards/embedly'];
}

export default helper(mobiledocCardNames);
